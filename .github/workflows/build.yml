name: Build ZeroTier MSI (Win7/2008R2 x64, Prometheus enabled, DevCmd fixed include)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2019

    env:
      CONFIGURATION: Release
      ARCH: x64
      VCPKG_ROOT: C:\vcpkg
      VCPKG_DEFAULT_TRIPLET: x64-windows

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1.3.1

      - name: Install CMake
        uses: lukka/get-cmake@latest
        with:
          cmakeVersion: 3.21.0

      - name: Install vcpkg (bootstrap)
        shell: pwsh
        run: |
          git clone https://github.com/microsoft/vcpkg $env:VCPKG_ROOT
          & "$env:VCPKG_ROOT\bootstrap-vcpkg.bat"

      - name: Install dependencies via vcpkg
        shell: pwsh
        run: |
          & "$env:VCPKG_ROOT\vcpkg.exe" install nlohmann-json:$env:VCPKG_DEFAULT_TRIPLET
          & "$env:VCPKG_ROOT\vcpkg.exe" install prometheus-cpp:$env:VCPKG_DEFAULT_TRIPLET
          $prefix = "C:\vcpkg\installed\x64-windows"
          if (!(Test-Path "$prefix\include\nlohmann\json.hpp")) { Write-Warning "nlohmann/json.hpp missing" }
          if (!(Test-Path "$prefix\include\prometheus")) { Write-Warning "prometheus headers missing from vcpkg" }
          Add-Content -Path $env:GITHUB_ENV -Value "VCPKG_PREFIX=$prefix"

      - name: Prepare include/library paths
        shell: pwsh
        run: |
          $prefix   = "C:\vcpkg\installed\x64-windows"
          $promBase = Join-Path "${{ github.workspace }}" "ext\prometheus-cpp-lite-1.0\simpleapi\include"

          Add-Content -Path $env:GITHUB_ENV -Value "PROM_BASE=$promBase"
          Add-Content -Path $env:GITHUB_ENV -Value "INCLUDE=$promBase;$prefix\include;$env:INCLUDE"
          Add-Content -Path $env:GITHUB_ENV -Value "LIB=$prefix\lib;$env:LIB"

      - name: Locate Visual Studio and DevCmd
        shell: pwsh
        run: |
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $vsPath = & $vswhere -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath
          $devCmd = Join-Path $vsPath "Common7\Tools\VsDevCmd.bat"
          "VSCMD=$devCmd" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Configure (VS2019, Win7 compatible, Prometheus enabled)
        shell: cmd
        run: |
          "%VSCMD%" && ^
          set PREFIX=%VCPKG_PREFIX% && ^
          set PROMBASE=%PROM_BASE% && ^
          set EXTRA_C_FLAGS=/EHsc && ^
          set EXTRA_CXX_FLAGS=/std:c++17 /Zc:__cplusplus /permissive- /EHsc /I%PROMBASE% /I%PREFIX%\include && ^
          set VS_GLOBALS=IncludePath=%PROMBASE%;%PREFIX%\include;%%(IncludePath);LibraryPath=%PREFIX%\lib;%%(LibraryPath) && ^
          cmake -S . -B build -G "Visual Studio 16 2019" -A %ARCH% -T v142 ^
            -DCMAKE_TOOLCHAIN_FILE=%VCPKG_ROOT%/scripts/buildsystems/vcpkg.cmake ^
            -DVCPKG_TARGET_TRIPLET=%VCPKG_DEFAULT_TRIPLET% ^
            -DCMAKE_C_STANDARD=11 ^
            -DCMAKE_C_STANDARD_REQUIRED=ON ^
            -DCMAKE_CXX_STANDARD=17 ^
            -DCMAKE_CXX_STANDARD_REQUIRED=ON ^
            -DCMAKE_CXX_EXTENSIONS=OFF ^
            -DWINVER=0x0601 ^
            -D_WIN32_WINNT=0x0601 ^
            -DZT_ENABLE_PROMETHEUS=ON ^
            -DCMAKE_C_FLAGS="%EXTRA_C_FLAGS%" ^
            -DCMAKE_CXX_FLAGS="%EXTRA_CXX_FLAGS%" ^
            -DCMAKE_VS_GLOBALS="%VS_GLOBALS%" ^
            -DCMAKE_INCLUDE_PATH="%PROMBASE%;%PREFIX%\include"

      - name: Build (cmake --build via DevCmd)
        shell: cmd
        run: |
          "%VSCMD%" && cmake --build build --config %CONFIGURATION% --parallel

      - name: Install WiX Toolset
        shell: pwsh
        run: choco install wix --version=3.11.2 -y

      - name: Generate installer.wxs
        shell: pwsh
        run: |
          $exePath = "build\Release\zerotier-one.exe"
          if (!(Test-Path $exePath)) { Write-Warning "Executable not found at $exePath" }
          New-Item -ItemType Directory -Force -Path installer | Out-Null
          $lines = @(
            '<?xml version="1.0" encoding="UTF-8"?>'
            '<Wix xmlns="http://schemas.microsoft.com/wix/2006/wi">'
            '  <Product Id="*" Name="ZeroTierOne" Language="1033" Version="1.0.0.0" Manufacturer="YourOrg" UpgradeCode="11111111-2222-3333-4444-555555555555">'
            '    <Package InstallerVersion="200" Compressed="yes" InstallScope="perMachine" />'
            '    <MediaTemplate />'
            '    <Directory Id="TARGETDIR" Name="SourceDir">'
            '      <Directory Id="ProgramFiles64Folder">'
            '        <Directory Id="INSTALLFOLDER" Name="ZeroTierOne">'
            '          <Component Id="MainExe" Guid="aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee">'
            "            <File Id='ZeroTierExe' Source='$exePath' KeyPath='yes' />"
            '          </Component>'
            '        </Directory>'
            '      </Directory>'
            '    </Directory>'
            '    <Feature Id="ProductFeature" Title="ZeroTierOne" Level="1">'
            '      <ComponentRef Id="MainExe" />'
            '    </Feature>'
            '  </Product>'
            '</Wix>'
          )
          $lines | Set-Content -Path installer\installer.wxs -Encoding UTF8

      - name: Package MSI
        shell: pwsh
        run: |
          candle installer\installer.wxs
          light installer\installer.wixobj -o ZeroTierOne-x64.msi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ZeroTierOne-x64
          path: |
            build/Release/zerotier-one.exe
            ZeroTierOne-x64.msi
